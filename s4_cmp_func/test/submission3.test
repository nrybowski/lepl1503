_id: 5c790b696779dd5666baa442
archive: 5c790b706779dd5666baa443
courseid: LSINF1252
custom: {}
grade: 50.0
input:
    '@lang': en
    '@random': []
    '@state': ''
    '@username': echristoph
    cmp_func: |-
        int cmp_func(uint8_t *func1(uint8_t n), uint8_t *func2(uint8_t n))
        {
            for(uint8_t i=0;i<256;i++){
                if(func1(i)!=func2(i)){return 0;}
            }
            return 1;
        }
problems:
    cmp_func:
    - failed
    - |+
        * Comparison between a function which tells if the argument is even and another which tells if it's odd

          => réussi (1/1) pts)


        * Comparison between two different functions which do the same computations

          => échoué (0/1) pts)

          Info: Your code exceeded the maximal allowed execution time.

        * Comparison between a function which returns 1 if the input is 42, 0 otherwise and another function which always returns 0

          => réussi (1/1) pts)


        * Comparison between the same function given twice

          => échoué (0/1) pts)

          Info: Your code exceeded the maximal allowed execution time.

response_type: rst
result: success
state: ''
status: done
stderr: ''
stdout: ''
submitted_on: 2019-03-01 11:37:29.442000
taskid: cmp_func
tests:
    timeout: true
text: |-
    - Votre code compile.

    - Il y a des erreurs dans votre solution.
username:
- echristoph
